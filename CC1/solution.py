def non_constructible_change(coins):
    if coins == []:
        return 1
    else:
        coins.sort()
        possible_sums = set()
        for i in range(len(coins)):
            for j in range(0, i):
                

    pass


